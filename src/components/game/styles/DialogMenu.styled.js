import styled from 'styled-components'
import Menu from '../Menu'

export const StyledDialogMenu = styled(Menu)`

    .buttons-bottom {
        margin-top: 15px;
        display: flex;
        justify-content: space-between;
    }
`